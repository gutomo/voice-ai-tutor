# DEMO_RUNBOOK.md — ライブデモ実行手順 (Phase 7)

> 10 から 12 分のライブデモを滞りなく回すための実行手順書。台本そのもの (時間配分と talking point) は
> `docs/DESIGN.md` §6 が一次資料。この文書は「当日どのボタンを押すか」「事前に何をリセットするか」
> 「詰まったときどう逃げるか」を足す。
>
> 当日の準備は `cd backend && uv run python -m app.preflight` の 1 本にまとまっている
> (DB 疎通 → デモデータのリセット → 台本の通しスモークを一括、緑/赤で判定。詳細は §1 手順 3)。
> API レベルの通しリハーサルは `backend/tests/test_demo_rehearsal.py` が自動化している (外部 API はスタブ)。
> 台本の各ビート (ドリル → ロールプレイ → 山場 → まとめメール) が順に成立することを毎回検証できる。

## 0. 当日の登場人物とデータ

- **Live Demo (Siswa)**: 商談で実際に録音するライブ用の学習者。`seed_all` が短いベースライン (ドリル 2 ターン、
  到達度 **53.4%**) を積んでおく。要フォロー閾値 (50%) のすぐ上なので、開始時はコホートの「要フォロー 2 名」を
  崩さず、当日 1 ターン録るだけで到達度が上へ動く (= money shot)。frontend の `SessionProvider` がこの同名
  学習者を再利用する (`DEMO_LEARNER_NAME = 'Live Demo (Siswa)'`)。
- **コホート 6 名**: Siti 74.4 / Dewi 64.8 / Budi 61.6 / Rina 54.4 / Andi 31.6 / Putra 26.8 (%)。
  Andi・Putra が要フォロー (< 50%)。Dewi が学習者ビューの見本 (推定 A2 / N4)。

## 1. デモ前の準備 (T-15 分)

1. **サービスを起動する** (3 つ。下 2 行は起動したまま常駐するので、各行を別ターミナルで)。
   ```bash
   docker compose up -d                                   # Postgres (常駐)
   cd backend && uv run uvicorn app.main:app --reload      # API (:8000、別ターミナルで常駐)
   cd frontend && npm run dev                              # フロント (:5173、別ターミナルで常駐)
   ```
   ヘルス確認: `curl http://localhost:8000/healthz` → `{"status":"ok"}`。

2. **環境変数を確認する** (`backend/.env`)。ライブで実採点・実メールまで見せるなら:
   `AZURE_SPEECH_KEY` / `AZURE_SPEECH_REGION` (発音採点)、`AWS_REGION` と IAM キー ＋ `BEDROCK_MODEL_ID`
   (会話・ルーブリック。推論プロファイル `us.anthropic.claude-sonnet-4-6` を使う。基盤 ID は on-demand 不可)、
   `EMAIL_SENDER` / `TEACHER_EMAIL` (SES。サンドボックスでは宛先も検証済みにする)。

3. **プリフライトを 1 本流す** (DB 疎通 → デモデータのリセット → 台本の通しスモークを一括、緑/赤で判定)。
   ```bash
   cd backend && uv run python -m app.preflight
   ```
   末尾が `GREEN: デモ準備 OK` なら当日回る。`RED` なら表示された `[FAIL]` を直す
   (最頻は Postgres 未起動 → 手順 1 の `docker compose up -d`)。出力中の
   `Live Demo (Siswa) ... passline= 53.4% ... <- ライブ用ベースライン` がライブの開始点。
   実 API (Azure / Bedrock / SES) の設定状況も advisory で並ぶ (未設定でも §4 のフォールバックでデモ可)。

   > 個別に叩くなら: `uv run python -m app.seed` (リセットのみ) と
   > `uv run pytest tests/test_demo_rehearsal.py -q` (スモークのみ) に分かれる。

4. **ブラウザを整える**。録音する端末 (スマホ or PC) で 📱 Siswa タブを開き、マイク許可を先に済ませておく
   (本番でパーミッションダイアログを出さない)。教師画面の出し方は 2 通り:
   - **単一画面 (推奨)**: 同じタブでアプリ内の 🎓 Guru に切り替える。切替のたび Dashboard は再マウントされて
     最新を自動取得し、セッションは切替をまたいで維持される (§2 5:00 の山場はこの経路。到達度は勝手に最新化する)。
   - **別画面で常時表示**: Guru を別タブ/別端末で開きっぱなしにするなら自動更新は効かない。ライブの 1 ターン後に
     コホート画面右上の **↻ Segarkan** を押して更新する (押さないと T-15 のままで到達度が動かない。学習者ビューに
     入っていると更新ボタンが出ないので、コホート画面に戻ってから押す)。

## 2. 当日の操作 (DESIGN §6 台本に対応)

| 時間 | 画面操作 | 補足 |
|---|---|---|
| 0:00 | 課題提示 (口頭) | 資料は不要。「アプリ不要・WABA 不要」を最初に言い切る。 |
| 0:30 | 📱 Siswa でドリルのモデル文を復唱 → 録音 → **発音スコアが画面に出る** | ここで **わざと少し外す** (§3)。ScoreCard の要練習語を指さす。 |
| 3:00 | ロールプレイを 1 ターン (AI が利用者役 田中さん)。応答を録音 | 二段採点 (STT → scripted) ＋ ルーブリック。返信音声が TTS で再生される。 |
| 5:00 | アプリ内の 🎓 Guru タブへ切替。**いまの Live Demo の到達度が上がっている** (**山場**) | 切替で Dashboard は最新を自動取得。セッションは維持され 📱 Siswa に戻れば続行できる。別画面運用なら先に ↻ Segarkan (§1 手順 4)。 |
| 7:00 | コホート画面。**要フォロー 2 名 (Andi・Putra)** の自動フラグ＋会話ログ | まとめメールの見本は Step 5 で実送信 (下記)。 |
| 8:00 | 📱 Siswa へ戻り「Akhiri sesi & kirim ringkasan」で終了 → メール送信 | 送信ログ (宛先・status) が画面に出る。学習者＋教師の 2 通。 |

> 到達度は「その学習者の全ターンの平均」から計算する。ライブで 1 ターン録ると 53.4% から上へ動く。
> 録音が失敗しても、押し直せば新しいターンとして保存されるだけで壊れない。

## 3. 「わざと少し外す」見せ場

発音採点の価値は「弱い音を具体的に指摘できる」こと。0:30 の復唱で、モデル文の難しい語を **意図的に少しだけ
崩して** 発音する。Azure がその語の accuracy を落とし、ScoreCard の要練習語と、教師ダッシュボードの発音
ヒートマップの両方に同じ語が出る。この 2 画面の一致が刺さる。

**どの語を崩すか**: turn 1「眠れましたか」か turn 2「測りますね」を選ぶ。この 2 語はライブ用学習者
(Live Demo) にあらかじめ要練習語として seed 済みなので (`_seed_live_demo`。ベースラインはドリル 2 ターンぶん)、
Azure がライブで崩れを拾えなくても教師ヒートマップに同じ語が残り、2 画面の一致が保たれる。turn 2 以降の
モデル文にある「体温」「血圧」でも山場のヒートマップは空にならない (他ターンの seed があるため) が、その語
自体は Azure が拾えたときだけ出る (Live Demo には seed していない)。

この一致 (ScoreCard の要練習語 → 永続化 → 教師ヒートマップ) は
`tests/test_demo_rehearsal.py::test_deliberate_miss_surfaces_in_scorecard_and_heatmap`
が検証している (turn 2 の録音サンプルで「体温」が両画面に出ることを確認)。

## 4. 詰まったときのフォールバック

- **発音スコアが出ない / Azure が遅い**: そのターンを飛ばし、シード済みの Dewi (🎓 Guru → 学習者ビュー) で
  発音ヒートマップ・到達度推移・会話ログを見せる。話の筋 (即時フィードバック → 教師が把握) は変わらない。
- **メールが届かない (SES サンドボックス)**: 送信ログの `status` (✅ Terkirim) と宛先を画面で見せれば「送った」
  ことは示せる (MessageId は API レスポンス側で、SessionBar の UI には出ない)。宛先は `TEACHER_EMAIL` / 学習者
  メールが未設定なら `EMAIL_SENDER` へフォールバックするので自分宛に届く。受信は後追いでも可。
- **セッションがおかしくなった**: 📱 Siswa タブをリロードする。`SessionProvider` が初期化され、次の録音で同じ
  Live Demo 学習者に新しいセッションが開く (学習者と過去ターンは DB に残る)。**ロールを切り替えてもセッションは
  維持される** (§2 5:00) ので、切替では作り直せない点に注意。1 ターンでも録れていれば「Akhiri sesi」→
  「Mulai sesi baru」でも開き直せる。最悪は §1 手順 3 のプリフライト (`--reset-only`) でシードから作り直す (数秒)。

## 5. デモ後のリセット

次の商談に備え、`cd backend && uv run python -m app.preflight --reset-only` を実行すれば
ライブ用学習者は 53.4% のベースラインへ戻る (当日足したライブのセッション・ターンごと作り直す。
スモークを省くので数秒)。コホート 6 名も同時に作り直される。

リセット後は 📱 Siswa タブをリロードする。リセットは学習者を作り直す (id が変わる) ので、フロントは
名前で引き直して新しいセッションを開く必要がある。リロードせず古いタブのまま録ると、消えたセッション ID を
掴んだままになり、採点・終了で 404 (「セッションが見つかりません」) になりうる。
